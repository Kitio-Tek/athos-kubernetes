apiVersion: pg.athos.io/v1alpha1
kind: PostgresCluster
metadata:
  name: example-cluster
  namespace: default
spec:
  postgresVersion: 16
  instances: 1
  storage:
    size: 10Gi
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: "1"
      memory: 1Gi
